# Ollama API Configuration
OLLAMA_URL=http://your-ollama-server:port/ollama/api/embed
AUTH_TOKEN=your-auth-token-here  # Optional: only include if your Ollama setup requires authentication
MODEL=your-embedding-model-name

# ChromaDB Configuration
CHROMA_URL=http://your-chromadb-server:port